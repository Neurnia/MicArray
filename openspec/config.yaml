schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.

context: |
  Main Goal: Read README.md.
  Log: doc/Log.md file contains what we have done until now.
  Only human can write this file.
  Style: doc/Style.md is the style guide of this project.
  All code files should follow this guide.

  IMPORTANT:
  - The AI must not run FPGA simulations (ModelSim) on its own.
  - Simulation requires human validation.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
